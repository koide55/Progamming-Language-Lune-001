module sample.basics

import java.time.LocalDate

def abs(x: Int): Int =
    if x < 0:
        -x
    else:
        x

def today(): IO[String] =
    IO:
        LocalDate.now().toString()

let inc: Int -> Int = fn x -> x + 1
let result = if true then inc(41) else 0

